# Windows Service Monitoring

A basic PowerShell script that checks the status of selected Windows services.

### What It Does

* Checks selected Windows services
* Displays whether services are running or stopped
* Identifies services that could require attention